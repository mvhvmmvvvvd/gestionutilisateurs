# Database connection settings
db.url=jdbc:mysql://localhost:3306/client_management
db.username=root
db.password=password

# Application settings
app.name=Client Management Application
app.version=1.0.0

# Logging settings
logging.level=INFO
logging.file=logs/application.log

# UI settings
ui.theme=Nimbus
ui.language=en